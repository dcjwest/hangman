import React from 'react';

const Header = () => {
    return (
        <header className="container">
            <h1>Hang-Man</h1>
            <p>Guess the hidden word!</p>
        </header>
    );
};

export default Header;
